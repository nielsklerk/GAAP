\chapter{Results}
This chapter begins with simulation results that demonstrate the reliability and limitations of the GAAP algorithm for determining the flux values of different sources. 

\section{Simulation}

\subsection{Deconvolution}\label{subsec: Deconvolution}
The deconvolution is stable when the weight is 'larger' than the PSF. In the simulation, this is straightforward to derive because both are Gaussian, so comparing their scale factors yields a direct measure of their relative sizes. The convolution of two Gaussians gives a Gaussian with a variance that is the sum of the variances of the original Gaussians. So the deconvolution of a weight function with a variance of $\sigma_\mathrm{weight}^2$ with a PSF with a variance of $\sigma_\mathrm{PSF}^2$ gives the deconvolved weight with a variance $\sigma^2_\mathrm{deconv}=\sigma^2_\mathrm{weight}-\sigma^2_\mathrm{PSF}$. From this, it is clear that the deconvolution fails when the weight is smaller than the PSF. The deconvolution was tested with different Gaussian weights and with a Gaussian fit to the results to estimate the scale parameter. The results are shown in \autoref{fig: deconvolution relation}. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{results/figures/simulation/deconvolution_relation.pdf}
    \caption{The deconvolution of Gaussian weight functions of different sizes by a Gaussian PSF. The deconvolved weights are fitted to find their standard deviation. This is compared to the expected result $\sigma^2_\mathrm{deconv}=\sigma^2_\mathrm{weight}-\sigma^2_\mathrm{PSF}$.}
    \label{fig: deconvolution relation}
\end{figure}
\autoref{fig: deconvolution relation} shows that the measured scale factors of the deconvolved weights align with the predicted value for weights that are larger than the PSF. 

Next, the stability of the deconvolution was tested for different image sizes. Due to the limited image size, larger weights relative to the image size begin to introduce artifacts in the deconvolution. Different weights were deconvolved for different image sizes. The deconvolution was then fitted, and the normalized RMS was calculated.

\begin{figure}
    \centering
    \includegraphics[width=0.8\linewidth]{results/figures/simulation/2_regimes.pdf}
    \caption{Caption}
    \label{fig: 2 regimes}
\end{figure}

\subsection{Flux Accuracy}

The true and observed flux values were compared across different PSF sizes and weights. The result is shown in \autoref{fig: flux accuracy}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{results/figures/simulation/flux_weight_psf_size0.pdf}
    \caption{The measured flux normalized by the true flux for different weights and PSF sizes. Flux measurements with weights smaller than the PSF are less accurate. The same holds for weights with a standard deviation $\lesssim 9\%$ of the image size. The yellow line denotes the border of 1$\%$ error}
    \label{fig: flux accuracy}
\end{figure}
The figure shows 2 regions where the measured flux values are inaccurate. The first region is the measurements taken with weights smaller than the PSF. This result is expected, as shown in \autoref{subsec: Deconvolution}. The second region comprises measurements above the PSF line and is $\gtrsim 10$ pixels wide. These incorrect measurements are due to the limited size of the weight function, leading to fringing. This can be mitigated by increasing the image size of the weight function.

\subsection{Flux Error Accuracy}
For each combination of the PSF and weight size, 10000 simulations were run to find the true error and the estimated error. 

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{results/figures/simulation/error_weight_psf_size.pdf}
    \caption{The measured error divided by the true flux error, computed from the distribution of flux values across different noise instances. This error is calculated for different PSF sizes. The PSF size and weight do not affect the accuracy of the error.}
    \label{fig: error PSF}
\end{figure}
The error is accurate and does not depend on the PSF size or the weight.

+ poisson noise

+ both Gaussian and Poisson noise

Next, the 1000 simulations are ran with different lag and correlation. 
\textbf{MAYBE EXAMPLES OF WHAT THE NOISE LOOKS LIKE FOR DIFFERENT NOISE SIZES}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{results/figures/simulation/error_covariance_size.pdf}
    \caption{The standard deviation of the flux values is accounted for by different correlations between the noise size and the lag, and by the pixel distance used for the variance estimation.}
    \label{fig: error covariance}
\end{figure}


\subsection{Aperture Shape}
The SNR for different weight function shapes was calculated. The results are shown in \autoref{fig: best aperture}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\linewidth]{results/figures/simulation/aperture_size.pdf}
    \caption{The SNR for different combinations of the horizontal and vertical standard deviations of the weight. The dashed line shows the spherical Gaussians; all the off-diagonal combinations are elliptical weights.}
    \label{fig: best aperture}
\end{figure}

The best weight is approximately equal to the intrinsic shape of the galaxy. This holds for both circular and elliptical shapes. Thus, the weight that yields the highest SNR is the one that best fits the intrinsic image. This can be estimated by fitting to an observation in a single field, preferably at the highest resolution, and then identifying the corresponding pre-seeing weight to apply to all bands.

\section{Observation}
The following analysis was done on the 53.00, -28.00 field. 

\subsection{Preprocessing}
\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{results/figures/observation/relation_pre_seeing_post_seeing.pdf}
    \caption{The relation between the size of the Gaussian weight function that maximizes some form of the SNR over all bands compared to the size of the source in the VIS field. }
    \label{fig: relation pre post seeing}
\end{figure}

\subsection{Observation Results: Flux Measurements}
Using MER for coordinates
Finding PSF
Fitting weights
Finding Noise square
Determining lag

comparing g,r,i, with DES-G,R,I

color color plot stars. gri using mer

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{results/figures/observation/color_color_euclid.pdf}
    \caption{Color-color diagram of the stars in the field 53.00 -28.00. The colors are derived from the flux in the DECam filters G, R, I. The sources in the image are those with a point-source probability> 90\%.}
    \label{fig:color_color_euclid}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{results/figures/observation/color_color_rubin.pdf}
    \caption{Color-color diagram of the stars in the field 53.00 -28.00. The colors are derived from the flux in the Rubin filters g, r, and i. The sources in the image are the objects in the MER-catalog with a point-source probability >90\%.}
    \label{fig:color_color_rubin}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{results/figures/observation/snr_comparison_uncorrelated.pdf}
    \caption{Comparison of flux measurements from GAAP and the MER catalog for all catalog filters. The dashed line denotes equal SNR.}
    \label{fig: snr comparison uncorrelated}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.9\linewidth]{results/figures/observation/snr_comparison_correlated.pdf}
    \caption{Comparison of flux measurements from GAAP and the MER catalog for all catalog filters. The dashed line denotes equal SNR.}
    \label{fig: snr comparison correlated}
\end{figure}


\subsection{Observation Results: Photometric Redshift}
parameters explained

compared redshift statistics derived from GAAP flux to those derived from MER

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{results/figures/analysis/z_distribution.pdf}
    \caption{Caption}
    \label{fig: z distribution}
\end{figure}

\begin{figure}[H]
    \centering
    \includegraphics[width=0.5\linewidth]{results/figures/analysis/z_phot_comparison.pdf}
    \caption{Caption}
    \label{fig: z phot comparison}
\end{figure}

spectroscopic redshift?


\citep{brammer_eazy_2008}